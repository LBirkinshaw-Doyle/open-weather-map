import AppName from './AppName';
import SearchBar from './SearchBar';

function Header() {
  return (
    <div id="header">
      <AppName />
      <SearchBar />
    </div>
  );
}

export default Header;
